spring:
  data:
    mongodb:
      #uri: mongodb://localhost:27017/fitnessactivity
      #database: fitnessactivity
      uri: mongodb://admin:admin123@localhost:27017/fitnessrecommendation?authSource=admin
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin123

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8383/eureka/

rabbitmq:
  exchange:
    name: fitness.exchange
  queue:
    name: activity.queue
  routing:
    key: activity.tracking


gemini:
  api:
    url: ${GEMINI_API_URL}
    key: ${GEMINI_API_KEY}
